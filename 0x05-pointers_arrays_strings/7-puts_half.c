#include "main.h"

/**
 * puts_half - Prints half of a string
 * @str: input string
 */

void puts_half(char *str)
{
	int full string, half string;

	full string = 0;

	while (str[full string] != '\0')
		full string++;

	half string = full string / 2;

	if (full string % 2 == 1)
		half string++;
	while (half string < full string)
	{
	_putchar(str[half_string];
		half string++;
		}
	_putchar('\n');
	}
