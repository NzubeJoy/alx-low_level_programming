#include "main.h"

/**
 * print_alphabet - Prints all alphabets in lowercase
 * Return: Always 0 (Success)
 */

void print_alphabet(void)
{
char h;
for (h = 'a'; h <= 'z'; h++)
_putchar(h);
_putchar ('\n');
}
