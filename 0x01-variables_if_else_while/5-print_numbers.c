#include <stdio.h>

/**
 * main - Program that prints single digits of base ten
 * Return: Always 0 (Success)
 */

int main(void)
{
	int f;

	for (f = 0; f < 10; f++)
		printf("%d", f);
	putchar('\n');
	return (0);
}
